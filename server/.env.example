# Copy this file to .env if you need to override defaults. .env is gitignored.

# URL of your local SearXNG instance (open-source, self-hosted, no API key needed).
# Start it with: cd searxng-instance && ./run.sh
SEARXNG_URL=http://127.0.0.1:8888

PORT=5001
